<!DOCTYPE html>
<html lang="en">
<head>
    <title>To-Do-List</title>
</head>
<body>
    <h1>My name is <%= name %></h1>
    <h1><%= date %></h1>

    <ul>
        
        <% for(var i=0; i<items.length; i++){ %>
            <li><%= items[i].name %></li>
        <% } %>

    </ul>

    <form action="/" method="POST">
        <input type="text" name="item">
        <button type="submit" name="button">click to add</button>
    </form>
</body>
</html>